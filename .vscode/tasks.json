{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Compile OpenGL C/C++",
            "type": "shell",
            "command": "gcc ${file} -o ${fileBasenameNoExtension}.out -lglut -lGL -lGLU -lm && ./${fileBasenameNoExtension}.out",
            "group": "build"
        }
    ]
}